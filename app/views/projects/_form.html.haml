= simple_form_for(@project) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :description
    = f.hidden_field :owner_id ,:value => current_user.id

  .form-actions
    = f.button :submit
